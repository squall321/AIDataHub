# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Test caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Build / output artifacts
output/
build/
dist/
*.log

# Environment / secrets
.env
.env.local
.env.*.local

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Word lock files
~$*.docx
~$*.xlsx
~$*.pptx

# tmp test outputs
tmp/

# Database files (only if locally created)
*.db
*.sqlite
*.sqlite3

# Docker volumes (if any local mount)
postgres_data/

# Node / VS Code extension
node_modules/
out/
*.vsix
*.tsbuildinfo
.vscode-test/

# Stray Windows junctions
NVIDIA Corporation/

# Local Claude Code state
.claude/

# bkit plugin local state (PDCA status, memory snapshots — regenerated per session)
.bkit/

# Unrelated tool dumps
antigravity/

# Large test pair binary corpora (regenerated locally; not for repo)
word_pairs/
pptx_pairs/
real_world_pptx/
rule_compliant_pptx/
pdf_pairs/
xlsx_pairs/

# Distribution build artifact (rebuilt from distribution/ on demand)
AI_Data_Distribution.zip

# Ingest data folders (per-deployment user content)
api_server/attachments/
api_server/figures/

# Stray editor copy
ai_data_strategy_deck copy.html

# /downloads 는 빌드 산출물 — setup.sh 가 생성, 커밋 금지
# (메타만 커밋되고 vsix 는 *.vsix 로 무시되면 fresh 서버에서 깨진 Download 버튼이 뜸)
api_server/static/downloads/
